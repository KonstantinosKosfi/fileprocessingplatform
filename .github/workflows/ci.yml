name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

permissions:
  contents: read

env:
  SPRING_PROFILES_ACTIVE: test
  BASE_PATH: ${{ github.workspace }}

jobs:
  build:
    runs-on: [self-hosted, Windows, X64]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"
          cache: gradle

      - name: Build and test
        run: .\gradlew.bat --no-daemon "-Dspring.profiles.active=test" build
